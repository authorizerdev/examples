# Authorizer Example with Next JS

## Local Setup

- Clone the repo `git clone https://github.com/authorizerdev/examples.git`
- Change directory to react JS `cd with-nextjs`
- Install dependencies `npm install`
- Start project `npm run dev`
